name: Deploy Social Flow to Development

on:
  push:
    branches: [dev, Confideleap]
  workflow_dispatch:

permissions:
  contents: read
  packages: write
  id-token: write

jobs:
  deploy:
    uses: ./.github/workflows/confideleap-deploy.yml
    with:
      environment: dev
      vps_user: dev-socialflow
