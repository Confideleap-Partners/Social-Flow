name: Deploy Social Flow to UAT

on:
  push:
    branches: [uat]
  workflow_dispatch:

permissions:
  contents: read
  packages: write
  id-token: write

jobs:
  deploy:
    uses: ./.github/workflows/confideleap-deploy.yml
    with:
      environment: uat
      vps_user: uat-socialflow
