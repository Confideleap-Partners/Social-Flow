name: opencode-review

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]

jobs:
  review:
    if: github.event.pull_request.changed_files > 0
    uses: Confideleap-Partners/.github/.github/workflows/opencode-review.yml@main
    secrets: inherit